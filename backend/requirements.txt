# LeadSynt backend — production dependencies
# Target database: Microsoft SQL Server (mssql+pyodbc).
# pyodbc ships binary wheels; the ODBC driver itself is installed at the
# deployment target (see docs/deployment.md). Dev profile uses SQLite.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
pydantic-settings>=2.2,<3.0
SQLAlchemy>=2.0.30,<2.1
alembic>=1.13,<2.0
PyJWT>=2.8,<3.0
bcrypt>=4.1,<5.0
email-validator>=2.1,<3.0
redis>=5.0,<6.0
celery>=5.4,<6.0
httpx>=0.27,<1.0
python-dateutil>=2.9,<3.0
pyodbc>=5.1; platform_system != "Darwin" or platform_machine != "arm64"
